This project's application is made using Anuglar
start up localhosting in WebApplication folder via command prompt by executing command "ng serve"

The backend service is built using .net and C#
said service can be run with the command "dotnet run" in the WebService folder in the command prompt

The web service utilises MongoDB with a cloud service at cloud.mongodb.com
if so desired access to the account to which the service is hooked up to is
username:decide833@gmail.com
password:Er51Z2sQ

